function scrollToSection(browse){
    document.getElementById(browse).scrollIntoView({
        behavior:'smooth'
    });
}
function scrollToSection(latest){
document.getElementById(latest).scrollIntoView({
    behavior:'smooth'
});
}